spring:
    cloud:
        config:
            label: master
            name: config-info
            profile: dev
            discovery:
                    enabled: true
                    service-id: ch11-9-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

